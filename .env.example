# EDXS gateway (Yarp.Gateway). Dev default is http://localhost:5001.
VITE_API_BASE_URL=http://localhost:5001

# One Window Job Portal, where candidates apply. Leave blank to fall back to
# the site's own contact page.
VITE_JOB_PORTAL_URL=
